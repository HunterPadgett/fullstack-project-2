<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Save The World</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Audiowide&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <nav class="navbar has-background-dark" id="mainNavBar" role="navigation" aria-label="main navigation">
    
    <div id="navbarBasicExample" class="navbar-menu">
      <div class="navbar-start">
        <h1 class="navbar-item title is-1 has-text-white" id="mainNavTitle">
          Cthulhu Invades
        </h1>
      </div>

      
     
      <div class="navbar-end">
        <div class="navbar-item">
          {{#if logged_in}}
          <div class="buttons">
            <a href="/home" class="button is-primary">
              <strong>Home</strong>
            </a>
            <a href="/charSelect" class="button is-primary">
              <strong>Select Character</strong>
            </a>
            <a href="/profile" class="button is-primary">
              <strong>Profile</strong>
            </a>
            <a class="button is-primary" id="logout">
              <strong>Logout</strong>
            </a>
            {{else}}
            <a href="/home" class="button is-primary">
              <strong>Home</strong>
            </a>
            {{/if}}  
          </div>
        </div>
      </div>
    </div>
  </nav>
    <!-- Render the sub layout -->
    {{{ body }}}
  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>